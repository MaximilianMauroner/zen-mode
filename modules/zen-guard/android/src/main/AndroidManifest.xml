<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <queries>
    <package android:name="com.google.android.youtube" />
    <package android:name="com.instagram.android" />
    <package android:name="com.twitter.android" />
    <!-- Lets the app limiter list launchable apps. Narrower than QUERY_ALL_PACKAGES. -->
    <intent>
      <action android:name="android.intent.action.MAIN" />
      <category android:name="android.intent.category.LAUNCHER" />
    </intent>
    <!-- Resolve only system handlers for the Settings escape path. -->
    <intent>
      <action android:name="android.settings.SETTINGS" />
    </intent>
    <intent>
      <action android:name="android.settings.ACCESSIBILITY_SETTINGS" />
    </intent>
    <intent>
      <action android:name="android.settings.ACCESSIBILITY_DETAILS_SETTINGS" />
    </intent>
  </queries>
  <application>
    <service
      android:name="com.maxmauroner.zenguard.ZenGuardAccessibilityService"
      android:description="@string/zen_guard_accessibility_service_description"
      android:exported="true"
      android:label="@string/zen_guard_accessibility_service_label"
      android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
      <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
      </intent-filter>
      <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/zen_guard_accessibility_service" />
    </service>
  </application>
</manifest>
